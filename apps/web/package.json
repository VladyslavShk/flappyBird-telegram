{
  "name": "flappy-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "npx http-server . -p 8080",
    "dev": "npx live-server --port=8080 --watch=."
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1"
  }
}
